let nomes = [2, 2]

for(let linha = 0; linha < 2; linha++) {
    for(let coluna = 0; coluna < 2; coluna++) {
        nomes[linha, coluna]
    }
}

for(let linha = 0; linha < 2; linha++) {
    for(let coluna = 0; coluna < 2; coluna++) {
        nomes[linha, coluna]

        console.log('s', 't', 'y', 'x')
    }
}

// 0.0 0.1
// 1.0 1.1