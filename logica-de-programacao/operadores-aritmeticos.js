// Adição = +
// Subtrair = -
// Multiplicação = *
// Divisão = /
// Resto = % (mod)
// Potência = Math.pow

// let adi, sub, mul, div, res, pot;

adi = 1 + 8
sub = 5 - 6
mul = 5 * 5
div = 5 / 2
res = 2 % 10
pot = Math.pow(5, 2)

console.log(`${adi}`)
console.log(`${sub}`)
console.log(`${mul}`)
console.log(`${div}`)
console.log(`${res}`)
console.log(`${pot}`)

// console.log(`+${adi}, -${sub}, *${mul}, /${div}, %${res}, Math.pow${pot}`)